<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wlzq.activity.redenvelope.dao.RedEnvelopeDao">
    
	<sql id="redEnvelopeColumns">
		a.id AS "id",
		a.user_id AS "userId",
		a.mobile AS "mobile",
		a.amount AS "amount",
		a.business_code AS "businessCode",
		a.business_no AS "businessNo",
		a.order_no AS "orderNo",
		a.status AS "status",
		a.check_status AS "checkStatus",
		a.notify_url AS "notifyUrl",
		a.recieve_url AS "recieveUrl",
		a.create_time AS "createTime",
		a.callback_time AS "callbackTime",
		a.callback_data AS "callbackData",
		a.remark AS "remark"
	</sql>
	
	<sql id="redEnvelopeJoins">
		LEFT JOIN sys_user u2 ON u2.id = a.user_id
	</sql>
    
	<select id="get" resultType="RedEnvelope">
		SELECT 
			<include refid="redEnvelopeColumns"/>
		FROM pay_red_envelope a
		<include refid="redEnvelopeJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findByOrderNo" resultType="RedEnvelope">
		SELECT 
			<include refid="redEnvelopeColumns"/>
		FROM pay_red_envelope a
		WHERE a.order_no = #{orderNo}
	</select>
	
	<select id="findByBusinessNo" resultType="RedEnvelope">
		SELECT 
			<include refid="redEnvelopeColumns"/>
		FROM pay_red_envelope a
		WHERE a.business_no = #{businessNo}
		and rownum = 1
	</select>
	
	<select id="findList" resultType="RedEnvelope">
		SELECT 
			<include refid="redEnvelopeColumns"/>
		FROM pay_red_envelope a
		<include refid="redEnvelopeJoins"/>
		<where>
			
			<if test="userId != null and userId != ''">
				AND a.user_id = #{userId}
			</if>
			<if test="mobile != null and mobile != ''">
				AND a.mobile = #{mobile}
			</if>
			<if test="businessNo != null and businessNo != ''">
				AND a.business_no = #{businessNo}
			</if>
			<if test="orderNo != null and orderNo != ''">
				AND a.order_no = #{orderNo}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="RedEnvelope">
		SELECT 
			<include refid="redEnvelopeColumns"/>
		FROM pay_red_envelope a
		<include refid="redEnvelopeJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert" >
		INSERT INTO pay_red_envelope(
			user_id,
			open_id,
			mobile,
			amount,
			business_code,
			business_no,
			order_no,
			status,
			check_status,
			notify_url,
			recieve_url,
			create_time,
			callback_time,
			send_data,
			callback_data,
			remark
		) VALUES (
			#{userId,jdbcType=VARCHAR},
			#{openId,jdbcType=VARCHAR},
			#{mobile,jdbcType=VARCHAR},
			#{amount,jdbcType=INTEGER},
			#{businessCode,jdbcType=VARCHAR},
			#{businessNo,jdbcType=VARCHAR},
			#{orderNo,jdbcType=VARCHAR},
			#{status,jdbcType=INTEGER},
			#{checkStatus,jdbcType=INTEGER},
			#{notifyUrl,jdbcType=VARCHAR},
			#{recieveUrl,jdbcType=VARCHAR},
			#{createTime, jdbcType=TIMESTAMP},
			#{callbackTime, jdbcType=TIMESTAMP},
			#{sendData,jdbcType=VARCHAR},
			#{callbackData,jdbcType=VARCHAR},
			#{remark,jdbcType=VARCHAR}
		)
	</insert>
	
	<update id="update">
		UPDATE pay_red_envelope SET 	
			status = #{status,jdbcType=INTEGER},
			callback_time = #{callbackTime, jdbcType=TIMESTAMP},
			pay_time = #{payTime, jdbcType=TIMESTAMP},
			callback_data = #{callbackData,jdbcType=VARCHAR}
		WHERE order_no = #{orderNo}
	</update>
	
</mapper>