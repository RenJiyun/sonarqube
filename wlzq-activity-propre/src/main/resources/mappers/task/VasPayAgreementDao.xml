<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wlzq.activity.task.dao.VasPayAgreementDao">

    <resultMap type="com.wlzq.activity.task.model.VasPayAgreement" id="vasPayAgreementMap">
        <result property="createTime" column="createTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="columns">
        a.product_code as productCode,
        a.customer_id as customerId,
        a.status as status,
        a.period_type as periodType,
        a.mobile as mobile,
        a.create_time as createTime
    </sql>

    <select id="findList" resultMap="vasPayAgreementMap">
        SELECT
        <include refid="columns"/>
        FROM VAS_PAY_AGREEMENT a
        <where>
            a.mobile = #{mobile} and a.product_code = #{productCode}
        </where>
    </select>
</mapper>